# Dependencies for the Chroma ingestion pipeline only.
# The application itself does not use any of these — see the repo root
# requirements.txt for app dependencies.
chromadb>=1.0
openai>=1.0
tiktoken>=0.7
tree-sitter>=0.23
tree-sitter-python>=0.23
python-dotenv>=1.0
# Optional, only needed for --provider sentence_transformers:
# sentence-transformers>=3.0
